# ------------------------------------------------------------------------------
# Core Application Configuration
# ------------------------------------------------------------------------------

# The base URL of your application. Required for generating absolute URLs.
# Example: https://yourapp.com
NEXT_PUBLIC_SITE_URL=

# E2B sandbox API key. Required for code execution.
# Get your key from https://e2b.dev/
E2B_API_KEY=

# ------------------------------------------------------------------------------
# Supabase Configuration (Authentication & Database)
# ------------------------------------------------------------------------------

# The public URL for your Supabase project.
NEXT_PUBLIC_SUPABASE_URL=

# The anonymous (public) key for your Supabase project.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# The service role key for your Supabase project. Used for admin-level operations.
# Keep this secret and only use it on the server-side.
SUPABASE_SERVICE_ROLE_KEY=

# ------------------------------------------------------------------------------
# LLM Provider API Keys (Choose the ones you want to use)
# ------------------------------------------------------------------------------

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=
FIREWORKS_API_KEY=
TOGETHER_API_KEY=
GOOGLE_AI_API_KEY=
MISTRAL_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=

# For Google Vertex AI, provide the full JSON credentials.
GOOGLE_VERTEX_CREDENTIALS=

# ------------------------------------------------------------------------------
# Stripe Configuration (Billing & Subscriptions)
# ------------------------------------------------------------------------------

# Your Stripe secret key.
STRIPE_SECRET_KEY=

# The secret for verifying Stripe webhook events.
STRIPE_WEBHOOK_SECRET=

# The Stripe Price ID for your "Pro" plan.
STRIPE_PRICE_ID_PRO=

# The Stripe Price ID for your "Enterprise" plan.
STRIPE_PRICE_ID_ENTERPRISE=

# ------------------------------------------------------------------------------
# Optional Features & Services
# ------------------------------------------------------------------------------

# Vercel KV / Upstash Redis (Used for rate limiting and short URLs)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# AWS S3 (Used for chat storage if configured)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_CHAT_BUCKET=codinit-chat-storage

# PostHog (Product Analytics)
NEXT_PUBLIC_ENABLE_POSTHOG=false
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Vercel Edge Config (Used for feature flags with GrowthBook)
EDGE_CONFIG=

# GrowthBook (Feature Flags)
GROWTHBOOK_API_ENDPOINT=
GROWTHBOOK_CLIENT_KEY=

# ZeroBounce (Email Validation)
ZEROBOUNCE_API_KEY=

# ------------------------------------------------------------------------------
# Rate Limiting Configuration
# ------------------------------------------------------------------------------

# Maximum number of requests allowed within the rate limit window.
RATE_LIMIT_MAX_REQUESTS=
# The duration of the rate limit window (e.g., "10s", "1m", "1h").
RATE_LIMIT_WINDOW=

# ------------------------------------------------------------------------------
# UI Feature Flags (Uncomment to enable)
# ------------------------------------------------------------------------------

# Hides the local models (like Ollama) from the model selection list.
# NEXT_PUBLIC_HIDE_LOCAL_MODELS=true

# Disables the ability for users to enter their own API key in the UI.
# NEXT_PUBLIC_NO_API_KEY_INPUT=true

# Disables the ability for users to enter a custom base URL for LLM providers.
# NEXT_PUBLIC_NO_BASE_URL_INPUT=true
